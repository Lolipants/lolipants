package com.lolipants.lolipants

import io.flutter.embedding.android.FlutterFragmentActivity

/// Android entry activity for the Flutter engine.
///
/// [FlutterFragmentActivity] is required for plugins that delegate to external
/// activities (camera, image picker, file picker) so results return reliably.
class MainActivity : FlutterFragmentActivity()
